{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.echo-labs.internal/echonet/memory_ingest_event.schema.json",
  "title": "EchoNet Memory Ingest Event",
  "description": "Specialization of EchoNet Event for Memory Layer anchor, zone, threshold, and ledger export signals. Requires source.system=memory_layer, domain=MemoryLayer, and event_type=memory.ingest.",
  "allOf": [
    {"$ref": "./echonet_event.schema.json"},
    {
      "properties": {
        "source": {
          "properties": {
            "system": {"const": "memory_layer"}
          }
        },
        "domain": {"const": "MemoryLayer"},
        "event_type": {"const": "memory.ingest"}
      }
    }
  ]
}
